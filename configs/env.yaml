gamma: 0.99            # discount factor
penalty_pull: 0.005    # penalty for pulling pitcher
max_smdp_horizon: 3    # 3-batter minimum
stay_action: 0         # action indexing
num_relievers: 10      # R
reward_column: "reward_folded"

# Behavioral cloning / offline RL settings
terminal_flags:
  half_inning_over: true
  game_over: true